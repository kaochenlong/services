version: "3.8"

services:
  redis:
    container_name: service_redis
    image: redis:6-alpine3.15
    restart: always
    ports:
      - 6379:6379
    volumes:
      - redis_store:/data

volumes:
  redis_store:
